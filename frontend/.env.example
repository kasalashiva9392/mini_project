# API base URL (no trailing slash). Required at build time for production.
# Use one active line. Local dev:
VITE_API_URL=http://localhost:5000
# Deployed frontend (example — uncomment and use your Render API URL):
# VITE_API_URL=https://mini-project-yycv.onrender.com
